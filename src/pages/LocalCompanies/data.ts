// This file previously contained mock data (COMPANIES array)
// Mock data has been removed as the application now uses real API data
// via React Query hooks in src/services/company/hooks.ts

// If you need to export types or utilities, add them here
// Otherwise, this file can be deleted or kept as a placeholder
